# セキュリティポリシー

## サポート方針

hachi-kanban は作者個人の実験的なプロジェクトです。

- **SLA はありません。** 報告への応答時間、修正の提供時期、修正そのものを約束しません
- サポート対象のバージョンは、`main` の最新コミットのみです。過去のコミットや
  タグに対する backport は行いません
- 商用利用・本番運用を前提とした保証はありません。ライセンス（MIT）どおり
  無保証で提供されます

それでも、深刻な問題の報告はありがたく受け取ります。

## 脆弱性の報告方法

**公開 Issue に脆弱性の詳細を書かないでください。**

GitHub の **private vulnerability reporting** を使ってください。

1. このリポジトリの **Security** タブを開く
2. **Report a vulnerability** から報告フォームを開く
3. 再現手順、影響範囲、確認した環境（OS、Node.js、pnpm のバージョン）を記載する

報告が非公開のまま作者に届きます。修正できた場合は、その経路で結果をお知らせします。

## 対象範囲

### 対象になるもの

- このリポジトリに含まれるコード（`packages/`、`scripts/`、`bin/`）の脆弱性
- 既定の設定のまま運用した場合に生じる、認証情報の漏洩・権限昇格・
  意図しないコード実行
- 既定の設定のまま `$HACHI_KANBAN_HOME` に書かれるファイルの権限不備

### 対象にならないもの

- 各利用者のローカル環境の設定・権限・ネットワーク構成に起因する問題
- 第三者製品の脆弱性（Node.js、pnpm、SQLite、`codex` / `claude` CLI、
  各 provider のサービス、OS など）。それぞれの提供元へ報告してください
- このリポジトリに含まれない外部 bridge サービスやアプライアンスの問題
- 依存パッケージの既知の脆弱性の単なる列挙（実際にこのコードから
  到達可能な経路を示してください）
- 設計上意図された挙動。worker エージェントはローカルでコマンドを実行します。
  これは仕様であり、信頼できるタスクだけを board に積む前提で運用してください

## 運用上の前提

このシステムは、自律型エージェントにローカルマシン上でコマンドを実行させます。
board に積まれたタスクの内容は、実質的にそのマシンでの実行権限と同じ重さを持ちます。
公開ネットワークに晒す設計にはなっていないため、Web 看板と supervisor は
信頼できるローカル環境でのみ動かしてください。
